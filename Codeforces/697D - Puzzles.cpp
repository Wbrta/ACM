#include <cstdio>
using namespace std;
int main()
{
	freopen("test.in", "r+", stdin);
	freopen("test.out", "w+", stdout);
	int n;
	while(~scanf("%d", &n)){
		for(int i = 2; i <= n; ++i){
			
		}
	}
	return 0;
}