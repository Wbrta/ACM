#include <cstdio>
#include <cstring>
using namespace std;
const int maxn = 1e6 + 10;
int next[maxn] = {0};
char str[maxn] = {0};
void getnext(){
	
}
int main()
{
	scanf("%s", str + 1);
	int len = strlen(str + 1);
	getnext();
	return 0;
}
