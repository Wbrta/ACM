#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
int main(){
    freopen("test.in", "r+", stdin);
    freopen("test.out", "w+", stdout);
    while(~scanf("%d", &n)){
        for(int i = 1; i <= n; ++i){
            for(int j = 1; j <= n; ++j){
                
            }
        }
    }
    return 0;
}