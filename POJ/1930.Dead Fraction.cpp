#include <cstring>
#include <iostream>
using namespace std;
const int maxn = 100 + 5;
char str[maxn];
int main(){
    ios::sync_with_stdio(false);
    while(cin >> str){
        int len = strlen(str);
        
        memset(str, 0, sizeof(str));
    }
    return 0;
}